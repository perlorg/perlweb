# -*- cperl -*-
eval 'exec $CBROOT/bin/perl -w $0 ${1+"$@"}'
  if my $running_under_some_shell;

use strict;
use lib $ENV{CBROOT} . "/lib";
use Combust::ApacheConfig;

my $ac = Combust::ApacheConfig->new;
$ac->generate_apache_configuration;




