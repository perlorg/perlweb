[%-  page = {
	    site  = 'qa.perl.org',
	    title = template.title,
            style = template.style or 'default.html'
            css   = 'http://new.x.perl.org/css/perl.css'
#<a href="/phalanx/">Phalanx</A> / 
	    sidebar = '
<a href="http://testers.cpan.org/">CPAN testers</a><br />
'
# XXX Do this correctly as a heredoc
    };

    # don't include any whitespace from here...
 #   RETURN;
-%]
