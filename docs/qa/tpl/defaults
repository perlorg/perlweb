[%-  page = {
	    site  = 'qa.perl.org',
	    title = template.title,
            style = template.style or 'default.html'
	    # css   = '/css/perl.css'  (default)
	    sidebar = '<a href="/phalanx/">Phalanx</A> / <a href="http://testers.cpan.org/">CPAN testers</a>'
    };

    # don't include any whitespace from here...
 #   RETURN;
-%]
