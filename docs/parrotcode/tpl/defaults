[%-  page = {
	    title = template.title,
            style = template.style or 'perl-directory.html',
	    site = "parrotcode",
	    banner_prefix = "parrotcode",
	    logobutton = { 
		src = "/images/parrot.small.png",
	        alt =   "parrot"
            },
	    tagline = "one bytecode to rule them all",
    };

    # don't include any whitespace from here...
 #   RETURN;
-%]

