[%-  page = {
            title = template.title,
            style = template.style or 'foundation.html',
	    site  = "The Perl Foundation",
	    siteinfo = "/contacts.html"
	    };

    # don't include any whitespace from here...
 #   RETURN;
-%]
