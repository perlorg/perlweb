Notes about *.perl.org site design

Structure...

<h1> - page title (set automatically from [% page.title %])

<h2> - page heading, intro sentence (optional)
<h3> - left for custom site high level (e.g. not currently used)
<h4> - section headings
<p>  - paragraph

<ul>
    <li>
        - style guide: these should NOT have a period at the end
    
<table class="content_table">
    - set up for two columns, you do not need anything else
    
<div class="box">
    - put a gray background around this div.
    
<code>perldoc perlfaq</code>
    - small code sample, inline