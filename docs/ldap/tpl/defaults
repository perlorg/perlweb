[%- 

    page.import({
        title    => 'Perl LDAP',
        style    => 'leo.html',
        site     => 'Perl-LDAP',
    });

    SET site = {
        name            => 'ldap.perl.org',
    }; 

    ldap = {
      version  = "0.39"
      released = "October 2008"
    };

    # don't include any whitespace from here...
    RETURN;
-%]
