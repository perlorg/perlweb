[%-  page = {
            title = template.title,
            style = template.style or 'default.html'
	    site  = "Perl-LDAP"
	    css   = "/site.css"
    };

    ldap = {
      version  = "0.32"
      released = "July 2004"
    };

    nofooter = 1;

    # don't include any whitespace from here...
 #   RETURN;
-%]
